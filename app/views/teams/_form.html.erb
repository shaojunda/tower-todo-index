<%= simple_form_for @team do |f| %>
  <div class="form-group">
    <%= f.input :name, label: "团队名称", input_html: { class: "form-control" } %>
  </div>
  <%= f.submit "创建团队", class: "btn btn-primary", data: { disable_with: "创建中..." } %>
<% end %>
