<%= simple_form_for [@team, @project] do |f| %>
  <div class="form-group">
    <%= f.input :name, label: "项目名称", input_html: { class: "form-control" } %>
  </div>
  <%= f.submit "创建项目", class: "btn btn-primary", data: { disable_with: "创建中..." } %>
<% end %>
